// config.js
// Set REACT_APP_BACKEND_URL in your .env file (e.g., http://localhost:5000 or your deployed backend URL)
const config = {
  BACKEND_URL: process.env.REACT_APP_BACKEND_URL,
};

export default config; 